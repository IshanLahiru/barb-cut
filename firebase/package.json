{
  "name": "firebase",
  "version": "1.0.1",
  "main": "functions/lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "cli": "npm run build && node functions/lib/cli.js",
    "migrate:up": "npm run cli -- migrate:up",
    "migrate:down": "npm run cli -- migrate:down",
    "migrate:status": "npm run cli -- migrate:status"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
